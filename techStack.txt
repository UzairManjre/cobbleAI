AI / ML

Qwen3.5-2B (fine-tuned) — core LLM
sentence-transformers/all-MiniLM-L6-v2 — embeddings
cross-encoder/ms-marco-MiniLM-L-6-v2 — reranker
Ollama (dev) / vLLM (prod) — model serving

Backend

FastAPI + Pydantic v2 — API layer
FastAPI-Users — auth
Motor — async MongoDB driver
slowapi — rate limiting
Celery — task queue
boto3 — S3 file handling
pdfplumber — PDF extraction
python-pptx — PPTX extraction
python-docx — DOCX extraction
nltk — sentence tokenization

Databases & Infrastructure

MongoDB — application state
Qdrant — vector database
Redis — Celery broker + cache + pub/sub + rate limit counters
MinIO (dev) / AWS S3 (prod) — file storage

Frontend

React 18 + Vite + TypeScript
Zustand — state management
React Flow — concept map
Tailwind CSS — styling
axios — API client
EventSource (native) — SSE streaming